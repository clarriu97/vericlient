[project]
name = "vericlient"
version = "0.3.0"
description = "Provides a simple and robust interaction with Veridas APIs."
authors = [
    {name = "Carlos Larriu", email = "larriucarlos@gmail.com"},
]
dependencies = [
    "pydantic>=2.13",
    "structlog>=25.4",
    "requests>=2.34.2",
    "pydantic-settings>=2.12",
]
requires-python = ">=3.11"
readme = "README.md"
license = { file = "LICENSE" }
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]

[project.urls]
Homepage = "https://vericlient.larri.dev/"
Documentation = "https://vericlient.larri.dev/"
Repository = "https://github.com/clarriu97/vericlient"
Issues = "https://github.com/clarriu97/vericlient/issues"
Changelog = "https://vericlient.larri.dev/CHANGELOG/"

[dependency-groups]
dev = [
    "ruff>=0.16",
    "pytest>=9.1",
    "pytest-cov>=7.1",
    "pytest-html>=4.2",
    "requests-mock>=1.12.1",
    "build>=1.3",
    "twine>=7.0",
    "pip-audit>=2.9",
]
docs = [
    # Capped below 2.0 on purpose: Material for MkDocs reports that MkDocs 2.0 removes the
    # plugin system and offers no migration path.
    "mkdocs>=1.6,<2",
    "mkdocs-material>=9.7",
    "mkdocstrings[python]>=1.0",
]

[tool.pdm.scripts]
lint = "ruff check ."
lint-fix = "ruff check . --fix"
format = "ruff format ."
format-check = "ruff format --check ."
docs-serve = "mkdocs serve"
docs-build = "mkdocs build --strict"
audit = { shell = """
    pdm export -dG:all -f requirements --without-hashes -o /tmp/vericlient-audit.txt \
    && pip-audit --requirement /tmp/vericlient-audit.txt --no-deps --disable-pip \
        --format=json --output=/tmp/vericlient-audit.json \
    ; python .github/scripts/audit_report.py /tmp/vericlient-audit.json
    """ }
test = """pytest tests --mock \
    --cov src/vericlient \
    --cov-report term --cov-fail-under 80 \
    --cov-report html:docs/_build/coverage \
    --cov-report xml:docs/_build/coverage/coverage.xml \
    --html=docs/_build/test-reports/index.html \
    --junitxml=docs/_build/test-reports/junit.xml \
    -o junit_suite_name=test"""
test-eu-sandbox = { shell = """
    pytest tests --env EU_SANDBOX -m $SERVICE \
    --html=docs/_build/test-reports/index.html \
    --junitxml=docs/_build/test-reports/junit.xml \
    -o junit_suite_name=test
    """ }
test-us-sandbox = { shell = """
    pytest tests --env US_SANDBOX -m $SERVICE \
    --html=docs/_build/test-reports/index.html \
    --junitxml=docs/_build/test-reports/junit.xml \
    -o junit_suite_name=test
    """ }
test-eu-production = { shell = """
    pytest tests --env EU_PRODUCTION -m $SERVICE \
    --html=docs/_build/test-reports/index.html \
    --junitxml=docs/_build/test-reports/junit.xml \
    -o junit_suite_name=test
    """ }
test-us-production = { shell = """
    pytest tests --env US_PRODUCTION -m $SERVICE \
    --html=docs/_build/test-reports/index.html \
    --junitxml=docs/_build/test-reports/junit.xml \
    -o junit_suite_name=test
    """ }
build = "python -m build"
publish = "twine upload dist/*"

[tool.ruff]
line-length = 130
indent-width = 4
target-version = "py311"
extend-exclude = ["docs/_build", "site"]

[tool.ruff.lint]
fixable = ["ALL"]
select = ["ALL"]
ignore = [
    # Conflicts with `ruff format`; the formatter already handles both.
    "COM812",   # Trailing comma missing
    "ISC001",   # Implicitly concatenated string literals on one line
    "CPY001",   # Missing copyright notice at top of file
    "D107",     # Missing docstring in `__init__`
    "D106",     # Missing docstring in public nested class
    "PTH123",   # `open()` should be replaced by `Path.open()`
    "PLR0913",  # Too many arguments in function definition
    "PLR0917",  # Too many positional arguments
    "PTH118",   # `os.path` should be replaced by `pathlib.Path`
    "PTH120",   # `os.path.dirname()` should be replaced by `Path.parent`
    "D203",     # 1 blank line required before class docstring
    "D213",     # multi-line-summary-second-line
    "D406",     # Section name should end with a newline
    "D407",     # Missing dashed underline after section
    "PTH119",   # `os.path.basename()` should be replaced by `Path.name`
]
unfixable = []

[tool.ruff.lint.per-file-ignores]
"tests/*.py" = [
    "S101",     # Use of assert detected
    "PLR2004",  # Magic value used in comparison: literal status codes read better here
    "ANN001",   # Missing type annotation for function argument
    "ANN201",   # Missing type annotation for public function
    "D100",     # Missing docstring in public module
    "D103",     # Missing docstring in public function
    "D104",     # Missing docstring in public package
]
".github/scripts/*.py" = [
    "INP001",   # File is part of an implicit namespace package
    "T201",     # `print` found: the report is meant to be read in the job log
]
"scripts/*.py" = [
    "INP001",   # File is part of an implicit namespace package
    "T201",     # `print` found: example scripts are meant to print
    "D100",     # Missing docstring in public module
    "S311",     # Pseudo-random generators are fine in examples
]

[tool.ruff.format]
quote-style = "double"

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[tool.pytest.ini_options]
markers = [
    "vcsp: marks tests as VCSP (deselect with '-m \"not vcsp\"')",
    "daspeak: marks tests as DASPEAK (deselect with '-m \"not daspeak\"')",
]
